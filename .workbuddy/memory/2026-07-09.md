# 2026-07-09

## cook by Receipt 全面代码检测 + SEO/GEO/竞品分析 (Round 1)
- 产品：AI 智能食材管家 + 零浪费餐规（React19+Vite+Express+Firebase+Gemini）。
- 代码质量：tsc --noEmit 通过、npm run build 通过（3.8s）。架构清晰、i18n(en/zh) 扎实。Gemini key 仅在 server.ts 后端读 env（未泄露前端），Firestore 规则 auth.uid==userId 正确。
- 上线阻断项(P0)：① 两 API 端点(/api/parse-ingredients,/api/generate-meal-plan)无鉴权无限流→Gemini账单/DoS风险 ② og-image/icon-192/512/favicon 全缺失 ③ /privacy /terms 是死链(server.ts app.get('*')全回退index.html，markdown未服务) ④ 无 robots.txt/sitemap.xml ⑤ 无 analytics/GSC。
- 中等问题：JS单包1.46MB(gzip406KB)无代码分割；纯SPA无可索引内容(SEO需内容层)；时区bug(toISOString存UTC跨时区差1天)；Firebase匿名登录本地兜底脆弱；无git仓库。
- SEO/GEO结论：当前SPA形态即使修技术项也近乎0自然流量，必须新建内容层(博客/食谱/工具页,SSR或static)才有长尾+AI引用增量。GEO靠结构化数据+llms.txt+原创数据+目录站收录。
- 竞品：SuperCook(占头部词)、KitchenPal(最像)、ZeroFoodWaste、Nosh、Samsung Food(原Whisk)；中文市场AI零浪费餐规细分近乎空白=机会窗口。

## P0 Launch Blockers — All Fixed ✅ (Round 2)

用户确认"修复"，5个P0全部完成：

1. **API鉴权+限流** ✅ — server.ts 重写：helmet v8 CSP(prod only)、三级限流(global 400/15min, parse 15/min, mealplan 5/min)、同源 guardAi 中间件(ALLOWED_ORIGINS env)。新增依赖 express-rate-limit@^7, helmet@^8
2. **品牌资产** ✅ — PIL生成：og-image.png(1200×630翡翠渐变+FC徽章+食材chip行), icon-512/192(圆角FC monogram), favicon.ico。index.html 已加 favicon link
3. **政策页** ✅ — public/privacy.html + terms.html 创建（翡翠主题独立HTML），server.ts 的 /privacy + /terms 路由已就位
4. **robots.txt + sitemap.xml** ✅ — public/ 下创建，Vite build 自动拷贝至 dist/
5. **Analytics + GSC** ✅ — src/main.tsx 注入 env-driven GA4(VITE_GA_ID)，index.html 加 GSC meta placeholder(commented-out)

**构建状态**: tsc PASS | vite build PASS (3.29s) | 10个新资产全部落地 dist/

**剩余 P1**: JS bundle 1.46MB(gzip 406KB) > 500KB 警告——需代码分割（上线不阻塞）

## A/C/D 优化组 — 全部完成 ✅ (Round 3)

用户指示"先做 a、c、d"（B=git 跳过）。

### A. 正确性/体验
- **A1 时区 bug ✅** — 新增 `src/lib/date.ts` 的 `toLocalDateStr(d)`（本地 YYYY-MM-DD，消除 toISOString() UTC 偏移，跨时区差1天问题）。替换 App.tsx(6处) + PantryTab.tsx(1处) 写侧 `.toISOString().split('T')[0]`；读侧 `new Date(item.expireAt)` → `new Date(item.expireAt+'T00:00:00')` (PantryTab,TodayTab)，TodayTab.getTodayDateStr 同步用 toLocalDateStr。
- **A2 生产环境清单 ✅** — 创建 `.env.example`：GEMINI_API_KEY(必填), ALLOWED_ORIGINS, NODE_ENV, PORT, 4个限流变量, VITE_GA_ID(可选)。
- **A3 错误透传 ✅** — CaptureModal.tsx + App.tsx 的 fetch 错误处理改为读取服务端 JSON 的 `error` 字段（429/403/500 具体文案透传给 UI），不再一律通用提示。

### C. 性能
- **C6 代码分割 ✅** — vite.config.ts 加 `build.rollupOptions.output.manualChunks`：firebase、recharts 拆独立长缓存 chunk。最终入口 chunk 从 1.46MB → 442KB(gzip 134KB vs 原406KB)；firebase 678KB(170KB gzip)、recharts 338KB(101KB gzip) 独立。先试了 react-vendor/motion 拆分会触发 circular chunk 警告，简化为只拆 firebase+recharts（无警告）。

### D. 增长向
- **D7 SEO 内容层 ✅** — 创建 `public/recipes.html`(6个真实零浪费食谱 hub) + `public/blog/reduce-food-waste.html`(~800词实用减废指南)，均含翡翠品牌风格内联CSS、Article schema JSON-LD、OG/Twitter 标签、内链互链 + 回 app。sitemap.xml 已加这两条 URL(priority 0.7, monthly)。
- **D9 llms.txt ✅** — `public/llms.txt` 描述 cook by Receipt 供 AI 引擎引用(GEO)。
- **D8 GSC** — 占位 meta 已在 Round 2 加(index.html commented-out)，用户此前表示暂不接 analytics；注册动作需用户自行在 GSC 完成。

**构建状态**: tsc PASS | vite build PASS (3.63s) | 新资产 dist/ 确认：recipes.html, blog/reduce-food-waste.html, llms.txt, robots.txt, sitemap.xml, 拆分 chunks 全到位。

**下一步可选**: ① git 初始化+首提交(用户暂跳过) ② 内容层扩展(更多食谱/工具页) ③ 接 GA4+GSC 激活 ④ 限时流提示之外的深度体验打磨。

## 应用内内容导航入口 ✅ (Round 4)
用户说"加上"= 把上轮提到的 in-app 内容导航入口做了（git 仍跳过）。
- `src/lib/translations.ts` 加 `recipesLink` / `blogLink`（en: "Zero-Waste Recipes"/"Food-Waste Guide"，zh: "零浪费食谱"/"减废指南"）。
- `src/App.tsx` footer 在 Privacy·Terms 旁加两链接：`/recipes.html` 与 `/blog/reduce-food-waste.html`，均 `target="_blank" rel="noopener noreferrer"`（新标签打开，保留 app 会话）。
- 内容页已有互链 + "返回 app /" 闭环，至此 SPA ↔ 静态内容页双向打通。
- 链接用 .html 后缀，与 sitemap.xml 中 URL 一致（prod 由 express.static 伺服，dev 由 Vite 伺服，均无需新路由）。
- 构建：tsc PASS | vite build PASS (3.68s) | 链接已确认进 dist JS bundle。

## 用户自建内容层 + 我复核 ✅ (Round 5)
用户自行补全了"更显眼入口 + 内容层扩量"两部分（此前我给过建议但说先不执行，用户自己做了）：
- **Explore 侧栏卡片**：App.tsx 加 4 条链接（Food Storage Guide / Zero-Waste Recipes / Storage Calculator / Meal Planning 101），克制常驻。
- **内容集群（public/ 下）**：guides/(food-storage-guide hub + 4 单食材页 eggs/chicken/spinach/herbs + meal-planning-101)、recipes/index.html(hub)、tools/(index hub + storage-calculator.html 交互工具，JS 抽成 storage-calculator.js)、blog/reduce-food-waste.html。
- **CSP 修法正确**：内容页 JS 抽成同源外部文件（content-faq.js / storage-calculator.js），helmet CSP 维持 `scriptSrc:["'self'"]`，无需弱化安全头。无 inline `<script>`/inline `onclick`。

### 我复核结论（8 项原坑全闭合）
1. CSP inline JS ✅ 已抽外部文件，prod 不再拦截工具页/FAQ。
2. /guides/ /tools/ 断链 ✅ 已补 index.html 枢纽页，JSON-LD 合法。
3. 重复食谱 hub ✅ 删除 recipes.html，统一 /recipes/。
4. 政策页回归 raw md ✅ server.ts 改 serving privacy.html/terms.html；所有 footer + sitemap 统一 /privacy /terms。
5. 博客孤立 ✅ 进 sitemap + guides/recipes hub 内链。
6. robots.txt ✅ 指向 sitemap.xml。
7. 构建 ✅ tsc PASS | vite build PASS (4.70s)，content 资产全进 dist/。
8. 无 .md 死链残留，内链一致。

### 留给用户的"意见"（均非上线硬伤）
- **Inter 字体未加载**：content-styles.css 写 'Inter' 但各页 head 无 @font-face/Google Fonts，回退系统字体（纯外观）。
- **public/privacy.md / terms.md 仍留着**：express.static 会直接伺服 /privacy.md 为 raw 文本，建议删或 gitignore（不影响 /privacy 路由）。
- **食谱集群只有 hub 无叶子页**：想靠食谱词拿排名需补单食谱详情页（内容缺口非 bug）。
- **GSC 仍未注册**：13+ 内容页已就位，但没提交 sitemap 给 Google 就看不见收录/排名——强烈建议上线即注册（免费、零侵入）。
- **GA4 仍待激活**（用户选择，不强制）。
